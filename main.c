#include <stdio.h>
#include <stdlib.h>
#define MAX_ELEVE 100
#define MAX 100
typedef struct {
    int numunique;
    char nom[MAX];
    char prenom[MAX];
    char notegenerale[MAX];
    float generalnote[MAX]
}student;

int e=0;
student students[MAX_ELEVE];
// 1. Ajoute un eleve :
void Ajouter_eleve(){

}

int main()
{
    return 0;
}
